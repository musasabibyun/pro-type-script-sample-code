---
description: 
globs: 
alwaysApply: false
---
指定したディレクトリで以下の操作を行ってください

1. `npx tsc --init`
2. tsconfig.json で以下の項目を修正
  a. "target": "es2020" に変更
  b. "module": "esnext" に変更
  c. "moduleResolution": "node" を有効
  d. "outDir": "./dist" を有効
  e. ファイルの最後に "include": ["./**/*.ts"] を追加